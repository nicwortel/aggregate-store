{
    "name": "nicwortel/aggregate-store",
    "description": "An experiment to create a PHP ORM specifically for DDD aggregates",
    "require": {
        "php": "^7.2"
    },
    "require-dev": {
        "nicwortel/coding-standard": "^1.1",
        "phpstan/phpstan": "^0.12.8"
    },
    "config": {
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "NicWortel\\AggregateStore\\": "src/"
        }
    }
}
